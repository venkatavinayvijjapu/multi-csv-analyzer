"""
core/
-----
Core infrastructure package for DataLens AI.

Modules:
    logging_config  — centralised logging setup
    sandbox         — secure, time-limited code execution
    code_validator  — AST-based static analysis of LLM-generated code
    data_quality    — schema normalisation + quality profiling
    join_detector   — cross-file join key detection
    memory          — per-session conversation history
    provenance      — query → data lineage tracking
    metrics         — accuracy and reliability metrics
"""
